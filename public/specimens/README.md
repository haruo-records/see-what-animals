# Specimen images

Drop real work images here (e.g. animal-024.png), then set the matching data
field to the path, e.g.:

  imageUrl: "/specimens/animal-024.png"

Until a real file exists, the app draws a deterministic SVG placeholder form
keyed by "specimen:<seed>" — no stock or AI imagery. See the root README,
"Replacing images".
